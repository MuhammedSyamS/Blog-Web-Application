<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container">

    <!-- 🌐 Brand -->
    <a class="navbar-brand fw-bold" href="/">
      <i class="bi bi-journal-text me-1"></i> Blogverse
    </a>

    <!-- Toggler for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
      aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

   

      <!-- Right side -->
      <ul class="navbar-nav ms-auto align-items-center">

        <% if (user) { %>
          <li class="nav-item me-3">
            <span class="text-light">👋 Hello, <strong><%= user.name %></strong></span>
          </li>

          <% if (user.role === 'admin') { %>
            <li class="nav-item me-2">
              <a href="/admin/dashboard" class="btn btn-warning btn-sm px-3 fw-semibold">
                <i class="bi bi-speedometer2 me-1"></i> Admin Dashboard
              </a>
            </li>
          <% } else { %>
            <li class="nav-item me-2">
              <a href="/user/profile" class="btn btn-outline-light btn-sm px-3">
                <i class="bi bi-person me-1"></i> My Profile
              </a>
            </li>
          <% } %>

          <li class="nav-item">
            <a href="/logout" class="btn btn-outline-light btn-sm px-3">Logout</a>
          </li>

        <% } else { %>
          <li class="nav-item">
            <a href="/login" class="btn btn-outline-light btn-sm px-3 me-2">Login</a>
          </li>
          <li class="nav-item">
            <a href="/signup" class="btn btn-primary btn-sm px-3 ">Sign Up</a>
          </li>
        <% } %>

      </ul>
    </div>
  </div>
</nav>
